🚀 RAILWAY DEPLOYMENT - QUICK START
=====================================

1. PUSH TO GITHUB
   git add .
   git commit -m "Deploy to Railway"
   git push origin main

2. DEPLOY ON RAILWAY
   - Go to railway.app
   - New Project → Deploy from GitHub
   - Select your repo

3. GENERATE DOMAIN
   - Settings → Networking → Generate Domain
   - Copy the URL

4. ADD ENVIRONMENT VARIABLES
   - Variables tab → Add all from .env.production.example
   - Replace NEXT_PUBLIC_URL with your Railway URL

5. ADD VOLUMES (CRITICAL!)
   railway volume add --mount-path /app/data
   railway volume add --mount-path /app/public/uploads

6. CONNECT CUSTOM DOMAIN (Optional)
   - Railway: Settings → Networking → Custom Domain
   - Namecheap: Advanced DNS → Add CNAME records
   - Wait 5-30 minutes

7. TEST
   - Visit your URL
   - Test /admin (password: admin123)
   - Create a blog post
   - Test contact form

✅ DONE! Your site is live!

Full guide: See DEPLOY.md
